# Frontend (en construccion)

Esta carpeta contendra la interfaz en React que consume la API del backend.

**Estado actual:** el backend ya expone `/api/search-test`. El frontend
se construira en la siguiente fase del proyecto, una vez el backend
tenga el flujo completo de IA (interpretacion + ranking).

Ver [../docs/ARCHITECTURE.md](../docs/ARCHITECTURE.md) para el diseno completo.
